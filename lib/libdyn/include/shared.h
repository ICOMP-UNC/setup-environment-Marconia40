#pragma once

/**
 * shared_lib_function
 * 
 * This function is defined in the shared library
 * 
 * @param msg: the message to be printed
 * @return the message size.
*/
int shared_lib_function(char* msg);
